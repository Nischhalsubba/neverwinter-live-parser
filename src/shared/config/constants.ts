export const DEFAULT_ENCOUNTER_INACTIVITY_TIMEOUT_MS = 30_000;
/**
 * Shared runtime constants.
 * Keeps cross-process defaults aligned between the renderer, main process,
 * worker imports, and parser engine.
 */
